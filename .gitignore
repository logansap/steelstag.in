# macOS
.DS_Store
._.DS_Store
._*

# Python
__pycache__/
*.pyc
*.pyo

# Backups
*.bak

# Archives (large, generated) - the released manufacturer package is an
# exception: it is a deliverable and must be versioned.
*.zip
!manufacturer/downloads/SteelStag-CompleteManufacturerPackage-SS2026-*.zip

# Released label and colour-reference artwork IS tracked. It ships to the
# factory, so it needs history and must be diffable across revisions.

# Temp/review folders
C:/Temp/
